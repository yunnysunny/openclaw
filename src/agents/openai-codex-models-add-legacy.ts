// Compat stub: branch's pi-embedded-runner/model.ts imports this from a
// path that upstream removed. Returning false skips the legacy compat path.
export function isLegacyModelsAddCodexMetadataModel(_params: unknown): boolean {
  return false;
}
