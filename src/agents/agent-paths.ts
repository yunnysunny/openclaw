// Compat re-export: branch's secrets/runtime.ts and other modules import
// resolveOpenClawAgentDir from "./agent-paths.js"; the actual definition
// lives in src/plugin-sdk/agent-dir-compat.ts post-merge.
export { resolveOpenClawAgentDir } from "../plugin-sdk/agent-dir-compat.js";
